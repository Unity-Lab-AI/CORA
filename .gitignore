# C.O.R.A - Cognitive Operations & Reasoning Assistant
# .gitignore - Prevent secret commits

# ============ SECRETS & CREDENTIALS ============
# API keys, tokens, credentials - NEVER COMMIT
*.key
*.pem
*.env
.env
.env.*
secrets.json
credentials.json
*_config.json
*.secret

# ============ PYTHON ============
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
ENV/
env/
.venv/

# ============ IDE & EDITOR ============
.idea/
.vscode/
*.swp
*.swo
*~
.project
.pydevproject
.settings/
*.sublime-workspace
*.sublime-project

# ============ OS FILES ============
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ============ LOGS & TEMP ============
logs/
*.log
*.tmp
*.temp
*.bak
*.backup

# ============ DATA & CACHE ============
data/*.json
!data/tasks.json.example
backups/
*.cache
.cache/

# ============ MODELS (large files) ============
models/
*.bin
*.pt
*.pth
*.onnx
*.safetensors

# ============ VOSK MODELS ============
vosk-model*/

# ============ TEST & COVERAGE ============
.coverage
htmlcov/
.tox/
.nox/
.pytest_cache/
tests/__pycache__/

# ============ JUPYTER ============
.ipynb_checkpoints/
*.ipynb_checkpoints

# ============ SPECIFIC TO CORA ============
# GitHub token storage (stored in user home, but just in case)
github_token.json
**/github_token.json
.cora/

# Sensitive config files
config/settings.json
config/personality.json

# User data
data/knowledge.json
data/tasks.json
data/chat_history.json

# Voice recordings (if any)
voice/recordings/
*.wav
*.mp3

# Screenshots and images (user data)
screenshots/
*.png
*.jpg
*.jpeg
!ui/assets/*.png
!docs/*.png

# ============ DOWNLOADED TOOLS ============
# The tools/ folder is ONLY for downloaded binaries (mpv, ffmpeg, etc)
# CORA source code tools are in cora_tools/
tools/
